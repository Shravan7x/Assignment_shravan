const express = require('express');
const router = express.Router();

// Placeholder - will be built in Step 4
router.get('/', (req, res) => {
  res.json({ message: 'Voucher routes coming soon' });
});

module.exports = router;
